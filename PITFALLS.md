# PITFALLS.md — 拼豆图纸工坊踩坑记录

## 技术栈

- uni-app + Vue 3 + Vite
- 目标平台：微信小程序
- 数据：本地存储，当前不依赖后端
- 构建命令：`npm run build:mp-weixin`
- 微信开发者工具目录：`dist/build/mp-weixin`

## 小程序约束

- 个人主体版本不做交易、会员、付费下载、公开社区和 UGC 投稿。
- 图片只在本地 canvas 处理，不上传服务器，降低隐私审核压力。
- 导出图片需要相册权限，失败时提示用户检查权限。
- Tab 页跳转使用 `switchTab`，非 Tab 页使用 `navigateTo`。

## 画布处理

- 图片转图纸通过隐藏 canvas：`drawImage` 后调用 `canvasGetImageData`。
- 大图尺寸上限当前设为 `64x64`，避免本地存储和编辑器渲染过重。
- 颜色匹配使用通用近似距离，不承诺等同某个品牌官方色卡。
- 导出图纸通过隐藏 canvas 绘制网格、图例和用豆清单。

## 本地存储

- Key：`perler_projects_v1`
- 最多保存 24 个作品。
- 作品包含：尺寸、色板、网格、统计、完成格子、创建/更新时间。

## 变现路线

- 第一阶段：先做免费工具，积累自然分享和搜索流量。
- 第二阶段：满足微信流量主条件后接入广告。
- 优先广告位：导出成功后、作品库空状态、色板页底部。
- 个人主体不要做材料售卖闭环。
